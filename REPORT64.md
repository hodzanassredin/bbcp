# BlackBox Component Builder — порт на 64 бита (amd64, Linux)

**Финальный отчёт — 2026-08-18**

## Статус: ядро порта ЗАВЕРШЁН

64-битная BlackBox собирается из исходников, запускается с GUI
(окна, меню, Log), компилирует и выполняет модули изнутри себя,
работает с кучей выше 4 ГБ, сборщик мусор корректен.
Все регрессионные пробы: **22/22 PASS**.

## Что сделано

- **Формат модулей OCF v2** — нативный 64-битный (без совместимости
  с 32 битами; эталон — прототип Hr).
- **Кодген amd64** (DevCPCamd64/CPLamd64/CPVamd64): REX.W, System V ABI,
  вызовы C и колбэки C→BB, нативный Int64 в одном регистре
  (эмуляция парами 32-битных регистров через FPU удалена).
- **Загрузчик bbrun64** (C): mmap модулей, fixup, резолюция импортов,
  режимы GUI и `--console`.
- **Ядро**: GC, куча >4 ГБ (проверено: 80×64МБ = 5ГБ + GC — OK),
  трап-машинерия на 64-битных указателях, INTO→JNO (INTO невалиден
  в amd64), TLS glibc — осознанный fail-fast (err 271).
- **GTK2-граница**: системные 64-битные glib/gtk, хэндлы LONGINT.
- **Бутстрап**: старт через 32-битный хост (bbcb2) с кросс-компилятором;
  теперь **bbcp самодостаточен** — BB64 конвертирует .odc и собирает
  себя сама (OdcTextU, sync-odc.sh, build-dev64.sh, test64.sh,
  mkworld64.sh, run-bb64).
- **Гигиена репо**: все .odc.txt в UTF-8, аудит INTEGER-указателей,
  все TODO64-маркеры разобраны, база знаний KB/ (23 файла).

## Принятые решения

1. Чистый 64-бит; 32-бит legacy выкидывается постепенно.
2. Временные костыли (MAP_32BIT) снимаются после настоящей починки.
3. Расширения (Json, Mcp, Crypto, _Http, Fjson, Async, Co_...) —
   отдельные репозитории, установка через Paket; порт каждого —
   отдельная задача.
4. Платформенные подсистемы (Win, Ole, Com, Ctl, Mdi, Fbsd, Nbsd, Obsd)
   сохраняются: целевые платформы — Windows, Linux, FreeBSD.

## Что дальше (варианты)

- Порт на Windows amd64 (PE-загрузчик + Win64 ABI в кодгене).
- Порт на FreeBSD amd64 (elf + libc-вызовы).
- Полировка Linux: minor — g_object_unref на выходе GUI, ld.so _dl_fini.
- Self-hosting полный цикл (BB64 собирает себя без dev0).

## Артефакты

- Репозиторий: `~/sources/bbcp` (origin — форк blackbox20).
- Собранный мир: `~/sources/bbcp64use`, запуск — `bbcp/run-bb64`.
- История проекта: `KB/PortHistory.md`; журнал: `tasks/task-64bits.md`.
